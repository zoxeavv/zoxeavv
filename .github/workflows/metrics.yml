name: GitHub metrics

on:
  schedule:
    - cron: "23 4 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: github-profile-metrics
  cancel-in-progress: true

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Render profile dashboard
        # Pinned to lowlighter/metrics master at 366f8b9 (2023-12-18).
        uses: lowlighter/metrics@366f8b9dfe3a59656c67d5dcad9950f59c9bc96d
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: zoxeavv
          filename: github-metrics.svg
          template: classic
          config_display: columns
          config_timezone: Europe/Paris
          config_twemoji: yes
          config_order: base.header, isocalendar, habits, languages, topics, repositories, base.activity+community
          output_action: commit
          committer_branch: main
          committer_message: "chore: refresh GitHub metrics"

          base: header, activity, community, metadata
          base_indepth: yes
          repositories_forks: no
          repositories_skipped: zoxeavv/zoxeavv, zoxeavv/HunyuanVideo-Avatar

          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          plugin_habits: yes
          plugin_habits_charts: yes
          plugin_habits_facts: yes

          plugin_languages: yes
          plugin_languages_sections: most-used, recently-used
          plugin_languages_details: percentage
          plugin_languages_limit: 6

          plugin_topics: yes
          plugin_topics_mode: icons
          plugin_topics_limit: 16

          plugin_repositories: yes
          plugin_repositories_pinned: 4
